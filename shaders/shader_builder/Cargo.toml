[package]
name = "shader_builder"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
spirv-builder = { git = "https://github.com/Rust-GPU/rust-gpu", package = "spirv-builder" }
